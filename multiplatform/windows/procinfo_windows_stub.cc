// Minimal libprocinfo for Windows x64 (no /proc). Symbols may be unused for -showversion.
#include "process.h"
namespace android {
namespace procinfo {
// Provide weak/no-op if headers require symbols - check process.h
}
}
